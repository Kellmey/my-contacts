import React from "react"; // imports react library

// Defining header
const Header = () => {
  return (
    <div className="Header">
        <h1>My Contacts</h1>
    </div>
  );
};

// exports Header
export default Header;